@{
    ViewData["Title"] = "Xác nhận quên mật khẩu";
}

<div class="forgot-password-confirmation-container">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card border-0 shadow-lg">
                    <div class="card-body p-5 text-center">
                        <div class="mb-4">
                            <i class="bi bi-envelope-check text-success" style="font-size: 4rem;"></i>
                        </div>
                        <h2 class="fw-bold mb-3">Kiểm tra email của bạn</h2>
                        <p class="mb-4">Chúng tôi đã gửi một liên kết đặt lại mật khẩu đến email của bạn. Vui lòng kiểm tra hộp thư đến và thư rác.</p>
                        
                        <div class="alert alert-info mb-4">
                            <i class="bi bi-info-circle me-2"></i>
                            Nếu bạn không nhận được email trong vòng vài phút, vui lòng kiểm tra thư mục spam hoặc thử lại.
                        </div>
                        
                        <div class="d-grid gap-3">
                            <a asp-action="ForgotPassword" class="btn btn-outline-primary">Thử lại</a>
                            <a asp-action="Login" class="btn btn-primary">Quay lại đăng nhập</a>
                        </div>
                    </div>
                </div>
                
                <div class="text-center mt-4">
                    <a asp-controller="Home" asp-action="Index" class="text-decoration-none">
                        <i class="bi bi-arrow-left me-1"></i> Quay lại trang chủ
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .forgot-password-confirmation-container {
        max-width: 450px;
        margin: 40px auto;
        padding: 30px;
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 0 20px rgba(0,0,0,0.1);
        text-align: center;
    }

    .confirmation-icon {
        font-size: 3rem;
        color: #28a745;
        margin-bottom: 20px;
    }

    .forgot-password-confirmation-container h2 {
        font-weight: bold;
        margin-bottom: 15px;
    }

    .forgot-password-confirmation-container p {
        color: #666;
        margin-bottom: 20px;
        line-height: 1.6;
    }

    .small-text {
        font-size: 0.9rem;
        color: #888;
    }

    .form-footer {
        margin-top: 25px;
    }

    .form-footer a {
        text-decoration: none;
    }
</style>